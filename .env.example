TELEGRAM_BOT_TOKEN=
PORT=3001

# leave both empty to use a local SQLite file (./data.db) instead of Turso
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# only needed for the Cloudflare webhook deployment (functions/telegram.js), not local dev
TELEGRAM_WEBHOOK_SECRET=
